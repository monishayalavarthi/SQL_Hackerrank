SELECT CITY, MIN(LENGTH(CITY))
FROM STATION
GROUP BY CITY
ORDER BY MIN(LENGTH(CITY)), CITY limit 1;

SELECT CITY, MAX(LENGTH(CITY))
FROM STATION
GROUP BY CITY
ORDER BY MAX(LENGTH(CITY)) DESC limit 1;
